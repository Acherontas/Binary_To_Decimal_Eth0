#ifndef BIN_TO_DEC_H
#define BIN_TO_DEC_H

#include <iostream>
#include <string>
#include <string.h>
using namespace std;

class bin_to_dec
{
    public:
        string fnl_rev;
        string rev_fix;
        int x=0;
        int one[4];
        int rev_one_ver[4];
        bin_to_dec();
        virtual ~bin_to_dec();
        bin_to_dec(const bin_to_dec& other);
        bin_to_dec& operator=(const bin_to_dec& other);
        string convert_bdec(string mystring);
        int clear_tmparr();

    protected:

    private:
};

#endif // BIN_TO_DEC_H
