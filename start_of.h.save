#ifndef START_OF_H
#define START_OF_H
#include <string>
#include <iostream>
using namespace std;

class start_of
{
    public:
        start_of();
        virtual ~start_of();
        start_of(const start_of& other);
        start_of& operator=(const start_of& other);
        int arr_of_ip[4];
        int ip_to_arr(string myipgw);
        string arofip;
        int bg=-1;
        int dvn_net(string dvn);
        string ndvn;
        int st_to_arr[4];
        int simple_to_arr(string ip);
    protected:

    private:
};

#endif // START_OF_H
